name: hangman_flutter
description: A multi-screen hangman game with dynamic categories
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0

flutter:
  uses-material-design: true
  assets:
    - assets/hangman/
    - assets/words.json
